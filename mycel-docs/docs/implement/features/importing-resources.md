# Importing resources

Note that this page is tied with the page [Node Formats](../resources/node/node-formats.md).

The diagram below covers the foundations of Mycel's import system: how a URL, a file, or raw text each get processed when calling the endpoint [Create Node](https://api.mycelcloud.com/scalar#tag/nodes/POST/collections/{col_id}/nodes).

![](../../assets/import-pipeline.excalidraw.svg)
